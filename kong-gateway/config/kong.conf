# Kong Configuration File - Minimal Setup

# Database settings
database = postgres
pg_host = kong-database
pg_port = 5432
pg_user = kong
pg_password = kongpass
pg_database = kong

# Network settings
proxy_listen = 0.0.0.0:8000
admin_listen = 0.0.0.0:8001

# Basic settings
log_level = info
nginx_daemon = off